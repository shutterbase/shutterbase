package client

/*
import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestUserLogin(t *testing.T) {
	c := NewClient("http://localhost:8090")
	assert.NotNil(t, c)

	ctx := context.Background()

	err := c.Login(ctx, "test.user@shutterbase.io", "test1234")
	assert.Nil(t, err)
	assert.NotEmpty(t, c.Auth.Token)
}

func TestGetImage(t *testing.T) {
	c := NewClient("http://localhost:8090")
	ctx := context.Background()
	_ = c.Login(ctx, "test.user@shutterbase.io", "test1234")

	image, err := c.GetImage(ctx, "dzjxowsxmsa8e09")
	assert.Nil(t, err)
	assert.NotNil(t, image)
	assert.Equal(t, "dzjxowsxmsa8e09", image.Id)
}
*/
