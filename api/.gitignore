.env
.vscode/secret-launch.json

# Generated by `just seed` / cmd/testserver: machine-local fixture manifests with
# freshly generated ids and a wall-clock stamp. Nothing reads them from a fixed
# path — the Go harness holds the manifest in memory.
seed-manifest.json
testserver-manifest.json
