// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/shutterbase/shutterbase/ent/predicate"
	"github.com/shutterbase/shutterbase/ent/project"
	"github.com/shutterbase/shutterbase/ent/projectassignment"
	"github.com/shutterbase/shutterbase/ent/role"
	"github.com/shutterbase/shutterbase/ent/user"
)

// ProjectAssignmentQuery is the builder for querying ProjectAssignment entities.
type ProjectAssignmentQuery struct {
	config
	ctx            *QueryContext
	order          []projectassignment.OrderOption
	inters         []Interceptor
	predicates     []predicate.ProjectAssignment
	withUser       *UserQuery
	withProject    *ProjectQuery
	withRole       *RoleQuery
	withCreatedBy  *UserQuery
	withModifiedBy *UserQuery
	withFKs        bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectAssignmentQuery builder.
func (paq *ProjectAssignmentQuery) Where(ps ...predicate.ProjectAssignment) *ProjectAssignmentQuery {
	paq.predicates = append(paq.predicates, ps...)
	return paq
}

// Limit the number of records to be returned by this query.
func (paq *ProjectAssignmentQuery) Limit(limit int) *ProjectAssignmentQuery {
	paq.ctx.Limit = &limit
	return paq
}

// Offset to start from.
func (paq *ProjectAssignmentQuery) Offset(offset int) *ProjectAssignmentQuery {
	paq.ctx.Offset = &offset
	return paq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (paq *ProjectAssignmentQuery) Unique(unique bool) *ProjectAssignmentQuery {
	paq.ctx.Unique = &unique
	return paq
}

// Order specifies how the records should be ordered.
func (paq *ProjectAssignmentQuery) Order(o ...projectassignment.OrderOption) *ProjectAssignmentQuery {
	paq.order = append(paq.order, o...)
	return paq
}

// QueryUser chains the current query on the "user" edge.
func (paq *ProjectAssignmentQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectassignment.Table, projectassignment.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, projectassignment.UserTable, projectassignment.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProject chains the current query on the "project" edge.
func (paq *ProjectAssignmentQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectassignment.Table, projectassignment.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, projectassignment.ProjectTable, projectassignment.ProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRole chains the current query on the "role" edge.
func (paq *ProjectAssignmentQuery) QueryRole() *RoleQuery {
	query := (&RoleClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectassignment.Table, projectassignment.FieldID, selector),
			sqlgraph.To(role.Table, role.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, projectassignment.RoleTable, projectassignment.RoleColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreatedBy chains the current query on the "created_by" edge.
func (paq *ProjectAssignmentQuery) QueryCreatedBy() *UserQuery {
	query := (&UserClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectassignment.Table, projectassignment.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, projectassignment.CreatedByTable, projectassignment.CreatedByColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryModifiedBy chains the current query on the "modified_by" edge.
func (paq *ProjectAssignmentQuery) QueryModifiedBy() *UserQuery {
	query := (&UserClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectassignment.Table, projectassignment.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, projectassignment.ModifiedByTable, projectassignment.ModifiedByColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProjectAssignment entity from the query.
// Returns a *NotFoundError when no ProjectAssignment was found.
func (paq *ProjectAssignmentQuery) First(ctx context.Context) (*ProjectAssignment, error) {
	nodes, err := paq.Limit(1).All(setContextOp(ctx, paq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projectassignment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) FirstX(ctx context.Context) *ProjectAssignment {
	node, err := paq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectAssignment ID from the query.
// Returns a *NotFoundError when no ProjectAssignment ID was found.
func (paq *ProjectAssignmentQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = paq.Limit(1).IDs(setContextOp(ctx, paq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projectassignment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := paq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectAssignment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectAssignment entity is found.
// Returns a *NotFoundError when no ProjectAssignment entities are found.
func (paq *ProjectAssignmentQuery) Only(ctx context.Context) (*ProjectAssignment, error) {
	nodes, err := paq.Limit(2).All(setContextOp(ctx, paq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projectassignment.Label}
	default:
		return nil, &NotSingularError{projectassignment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) OnlyX(ctx context.Context) *ProjectAssignment {
	node, err := paq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectAssignment ID in the query.
// Returns a *NotSingularError when more than one ProjectAssignment ID is found.
// Returns a *NotFoundError when no entities are found.
func (paq *ProjectAssignmentQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = paq.Limit(2).IDs(setContextOp(ctx, paq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projectassignment.Label}
	default:
		err = &NotSingularError{projectassignment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := paq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectAssignments.
func (paq *ProjectAssignmentQuery) All(ctx context.Context) ([]*ProjectAssignment, error) {
	ctx = setContextOp(ctx, paq.ctx, "All")
	if err := paq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectAssignment, *ProjectAssignmentQuery]()
	return withInterceptors[[]*ProjectAssignment](ctx, paq, qr, paq.inters)
}

// AllX is like All, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) AllX(ctx context.Context) []*ProjectAssignment {
	nodes, err := paq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectAssignment IDs.
func (paq *ProjectAssignmentQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if paq.ctx.Unique == nil && paq.path != nil {
		paq.Unique(true)
	}
	ctx = setContextOp(ctx, paq.ctx, "IDs")
	if err = paq.Select(projectassignment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := paq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (paq *ProjectAssignmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, paq.ctx, "Count")
	if err := paq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, paq, querierCount[*ProjectAssignmentQuery](), paq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) CountX(ctx context.Context) int {
	count, err := paq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (paq *ProjectAssignmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, paq.ctx, "Exist")
	switch _, err := paq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (paq *ProjectAssignmentQuery) ExistX(ctx context.Context) bool {
	exist, err := paq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectAssignmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (paq *ProjectAssignmentQuery) Clone() *ProjectAssignmentQuery {
	if paq == nil {
		return nil
	}
	return &ProjectAssignmentQuery{
		config:         paq.config,
		ctx:            paq.ctx.Clone(),
		order:          append([]projectassignment.OrderOption{}, paq.order...),
		inters:         append([]Interceptor{}, paq.inters...),
		predicates:     append([]predicate.ProjectAssignment{}, paq.predicates...),
		withUser:       paq.withUser.Clone(),
		withProject:    paq.withProject.Clone(),
		withRole:       paq.withRole.Clone(),
		withCreatedBy:  paq.withCreatedBy.Clone(),
		withModifiedBy: paq.withModifiedBy.Clone(),
		// clone intermediate query.
		sql:  paq.sql.Clone(),
		path: paq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *ProjectAssignmentQuery) WithUser(opts ...func(*UserQuery)) *ProjectAssignmentQuery {
	query := (&UserClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withUser = query
	return paq
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *ProjectAssignmentQuery) WithProject(opts ...func(*ProjectQuery)) *ProjectAssignmentQuery {
	query := (&ProjectClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withProject = query
	return paq
}

// WithRole tells the query-builder to eager-load the nodes that are connected to
// the "role" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *ProjectAssignmentQuery) WithRole(opts ...func(*RoleQuery)) *ProjectAssignmentQuery {
	query := (&RoleClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withRole = query
	return paq
}

// WithCreatedBy tells the query-builder to eager-load the nodes that are connected to
// the "created_by" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *ProjectAssignmentQuery) WithCreatedBy(opts ...func(*UserQuery)) *ProjectAssignmentQuery {
	query := (&UserClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withCreatedBy = query
	return paq
}

// WithModifiedBy tells the query-builder to eager-load the nodes that are connected to
// the "modified_by" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *ProjectAssignmentQuery) WithModifiedBy(opts ...func(*UserQuery)) *ProjectAssignmentQuery {
	query := (&UserClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withModifiedBy = query
	return paq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectAssignment.Query().
//		GroupBy(projectassignment.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (paq *ProjectAssignmentQuery) GroupBy(field string, fields ...string) *ProjectAssignmentGroupBy {
	paq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectAssignmentGroupBy{build: paq}
	grbuild.flds = &paq.ctx.Fields
	grbuild.label = projectassignment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//	}
//
//	client.ProjectAssignment.Query().
//		Select(projectassignment.FieldCreatedAt).
//		Scan(ctx, &v)
func (paq *ProjectAssignmentQuery) Select(fields ...string) *ProjectAssignmentSelect {
	paq.ctx.Fields = append(paq.ctx.Fields, fields...)
	sbuild := &ProjectAssignmentSelect{ProjectAssignmentQuery: paq}
	sbuild.label = projectassignment.Label
	sbuild.flds, sbuild.scan = &paq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectAssignmentSelect configured with the given aggregations.
func (paq *ProjectAssignmentQuery) Aggregate(fns ...AggregateFunc) *ProjectAssignmentSelect {
	return paq.Select().Aggregate(fns...)
}

func (paq *ProjectAssignmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range paq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, paq); err != nil {
				return err
			}
		}
	}
	for _, f := range paq.ctx.Fields {
		if !projectassignment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if paq.path != nil {
		prev, err := paq.path(ctx)
		if err != nil {
			return err
		}
		paq.sql = prev
	}
	return nil
}

func (paq *ProjectAssignmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectAssignment, error) {
	var (
		nodes       = []*ProjectAssignment{}
		withFKs     = paq.withFKs
		_spec       = paq.querySpec()
		loadedTypes = [5]bool{
			paq.withUser != nil,
			paq.withProject != nil,
			paq.withRole != nil,
			paq.withCreatedBy != nil,
			paq.withModifiedBy != nil,
		}
	)
	if paq.withUser != nil || paq.withProject != nil || paq.withRole != nil || paq.withCreatedBy != nil || paq.withModifiedBy != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, projectassignment.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectAssignment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectAssignment{config: paq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, paq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := paq.withUser; query != nil {
		if err := paq.loadUser(ctx, query, nodes, nil,
			func(n *ProjectAssignment, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := paq.withProject; query != nil {
		if err := paq.loadProject(ctx, query, nodes, nil,
			func(n *ProjectAssignment, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	if query := paq.withRole; query != nil {
		if err := paq.loadRole(ctx, query, nodes, nil,
			func(n *ProjectAssignment, e *Role) { n.Edges.Role = e }); err != nil {
			return nil, err
		}
	}
	if query := paq.withCreatedBy; query != nil {
		if err := paq.loadCreatedBy(ctx, query, nodes, nil,
			func(n *ProjectAssignment, e *User) { n.Edges.CreatedBy = e }); err != nil {
			return nil, err
		}
	}
	if query := paq.withModifiedBy; query != nil {
		if err := paq.loadModifiedBy(ctx, query, nodes, nil,
			func(n *ProjectAssignment, e *User) { n.Edges.ModifiedBy = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (paq *ProjectAssignmentQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ProjectAssignment, init func(*ProjectAssignment), assign func(*ProjectAssignment, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectAssignment)
	for i := range nodes {
		if nodes[i].project_assignment_user == nil {
			continue
		}
		fk := *nodes[i].project_assignment_user
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_assignment_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (paq *ProjectAssignmentQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*ProjectAssignment, init func(*ProjectAssignment), assign func(*ProjectAssignment, *Project)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectAssignment)
	for i := range nodes {
		if nodes[i].project_assignment_project == nil {
			continue
		}
		fk := *nodes[i].project_assignment_project
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_assignment_project" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (paq *ProjectAssignmentQuery) loadRole(ctx context.Context, query *RoleQuery, nodes []*ProjectAssignment, init func(*ProjectAssignment), assign func(*ProjectAssignment, *Role)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectAssignment)
	for i := range nodes {
		if nodes[i].project_assignment_role == nil {
			continue
		}
		fk := *nodes[i].project_assignment_role
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(role.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_assignment_role" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (paq *ProjectAssignmentQuery) loadCreatedBy(ctx context.Context, query *UserQuery, nodes []*ProjectAssignment, init func(*ProjectAssignment), assign func(*ProjectAssignment, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectAssignment)
	for i := range nodes {
		if nodes[i].project_assignment_created_by == nil {
			continue
		}
		fk := *nodes[i].project_assignment_created_by
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_assignment_created_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (paq *ProjectAssignmentQuery) loadModifiedBy(ctx context.Context, query *UserQuery, nodes []*ProjectAssignment, init func(*ProjectAssignment), assign func(*ProjectAssignment, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectAssignment)
	for i := range nodes {
		if nodes[i].project_assignment_modified_by == nil {
			continue
		}
		fk := *nodes[i].project_assignment_modified_by
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_assignment_modified_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (paq *ProjectAssignmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := paq.querySpec()
	_spec.Node.Columns = paq.ctx.Fields
	if len(paq.ctx.Fields) > 0 {
		_spec.Unique = paq.ctx.Unique != nil && *paq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, paq.driver, _spec)
}

func (paq *ProjectAssignmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projectassignment.Table, projectassignment.Columns, sqlgraph.NewFieldSpec(projectassignment.FieldID, field.TypeUUID))
	_spec.From = paq.sql
	if unique := paq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if paq.path != nil {
		_spec.Unique = true
	}
	if fields := paq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projectassignment.FieldID)
		for i := range fields {
			if fields[i] != projectassignment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := paq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := paq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := paq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := paq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (paq *ProjectAssignmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(paq.driver.Dialect())
	t1 := builder.Table(projectassignment.Table)
	columns := paq.ctx.Fields
	if len(columns) == 0 {
		columns = projectassignment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if paq.sql != nil {
		selector = paq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if paq.ctx.Unique != nil && *paq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range paq.predicates {
		p(selector)
	}
	for _, p := range paq.order {
		p(selector)
	}
	if offset := paq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := paq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProjectAssignmentGroupBy is the group-by builder for ProjectAssignment entities.
type ProjectAssignmentGroupBy struct {
	selector
	build *ProjectAssignmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pagb *ProjectAssignmentGroupBy) Aggregate(fns ...AggregateFunc) *ProjectAssignmentGroupBy {
	pagb.fns = append(pagb.fns, fns...)
	return pagb
}

// Scan applies the selector query and scans the result into the given value.
func (pagb *ProjectAssignmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pagb.build.ctx, "GroupBy")
	if err := pagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectAssignmentQuery, *ProjectAssignmentGroupBy](ctx, pagb.build, pagb, pagb.build.inters, v)
}

func (pagb *ProjectAssignmentGroupBy) sqlScan(ctx context.Context, root *ProjectAssignmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pagb.fns))
	for _, fn := range pagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pagb.flds)+len(pagb.fns))
		for _, f := range *pagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectAssignmentSelect is the builder for selecting fields of ProjectAssignment entities.
type ProjectAssignmentSelect struct {
	*ProjectAssignmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pas *ProjectAssignmentSelect) Aggregate(fns ...AggregateFunc) *ProjectAssignmentSelect {
	pas.fns = append(pas.fns, fns...)
	return pas
}

// Scan applies the selector query and scans the result into the given value.
func (pas *ProjectAssignmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pas.ctx, "Select")
	if err := pas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectAssignmentQuery, *ProjectAssignmentSelect](ctx, pas.ProjectAssignmentQuery, pas, pas.inters, v)
}

func (pas *ProjectAssignmentSelect) sqlScan(ctx context.Context, root *ProjectAssignmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pas.fns))
	for _, fn := range pas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
